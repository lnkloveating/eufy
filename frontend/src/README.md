# Frontend source layout

该目录目前仅为占位，不包含实现。

- `app/`：应用装配与路由
- `features/`：Agent、告警、事件、设备、审计等功能模块
- `components/`：通用 UI
- `lib/`：API client 与基础设施
- `types/`：后端契约类型
- `styles/`：全局样式和设计令牌

后端 API 稳定前不创建页面或模拟业务逻辑。
